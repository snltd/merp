#!/bin/sh

# Runs native zone tests in an extant bhyve zone

MERP_ROOT=$(readlink -e ${0%/*}/../..)
ZONE_IP=$(sed -n '/global-test-zone-ip/s/.*\"\(.*\)\".*/\1/p' \
  ${MERP_ROOT}/doers/zones/site.janet)

ssh root@${ZONE_IP} /merp/doers/bin/run-native-tests
